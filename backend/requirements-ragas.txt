# Optional eval-only dependencies for RAGAS smoke tests.
# Keep these out of backend/requirements.txt so the OpenAI SDK runtime does not
# regain eval-framework transitive dependencies in normal installs.
ragas>=0.3.9,<0.4.0
